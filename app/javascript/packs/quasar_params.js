// Quasar framework
import iconSet from 'quasar/icon-set/fontawesome-v5.js'

import {
    QLayout,
    QHeader,
    QDrawer,
    QPageContainer,
    QPage,
    QBtn
} from 'quasar'

export default {
    config: {},
    components: {
        QLayout,
        QHeader,
        QDrawer,
        QPageContainer,
        QPage,
        QBtn
    },
    directives: {
    },
    iconSet: iconSet,
    plugins: {
    }
}
